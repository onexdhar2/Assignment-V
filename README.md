# Assignment-05: GitHub Issues Tracker

- Create a readme file and answer this question on your own. Don’t copy-paste from Google or any AI chatbot. 
    - 1️⃣ What is the difference between var, let, and const?
    var is global and reusable.let is block level element and reusable.const is block level and it can declare one time.
    - 2️⃣ What is the spread operator (...)?
    speard operation convert multiple element to one element.
    - 3️⃣ What is the difference between map(), filter(), and forEach()?
    map(), filter() is a mathod of array in javascript.map() do modify and creat a new array.filter() can select specific array and return.
    forEach is a method loop.it use over array and return output.
    - 4️⃣ What is an arrow function?
    arrow function is a shorten method of declare function.here is the example.
    (param)=>{

    }
    - 5️⃣ What are template literals?
    template literals is a shorten version of creat string.It helps creat dynamic string where we use multi line string with ${}.  



